name: Secret Protection Test

on:
  workflow_dispatch: # This is the manual "Start Button"

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Attempt to show secret
        run: |
          echo "The secret is: ${{ secrets.MY_SENSITIVE_TOKEN }}"
